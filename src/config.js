import 'universal-fetch';

const config = {
  bodyLimit: '50mb',
  port: 8080,
  dbHost: '35.221.200.40',
  dbScheme: 'mathbooking',
  dbUser: 'darkforte',
  dbPassword: '@Qwerty01',
  disableSqlLog: true,
};

export default config;
